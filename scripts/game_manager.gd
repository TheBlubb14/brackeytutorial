extends Node

# Issue: When reloading scene the score_label 
#var score = 0
#@onready var score_label = get_node("/root/Game/%ScoreLabel")
#
#func add_point():d
	#score += 1
	#score_label.text = "You collected " + str(score) + " coins."
	#print(score)
